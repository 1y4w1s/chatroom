{
  "name": "chatroom-root",
  "version": "1.0.0",
  "description": "Chatroom application root",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && npm run dev"
  },
  "engines": {
    "node": "20.x"
  }
}
